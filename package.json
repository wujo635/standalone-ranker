{
  "name": "standalone-ranker",
  "private": true,
  "description": "Dev-only tooling for the single-file Ranker app (index.html needs no build or install).",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^30.1.1"
  }
}
